// forin loop mostly used with objects

const myObject = {
    js: 'javascript',
    py: 'python',
    rb: 'ruby',
    swift: 'swift for apple'
}

for (const key in myObject) {
   
  //  console.log(`${key} is short for ${myObject[key]}`)
    
}
    

const myArray = ["javascript", "python",  "ruby", "cpp"]
for (const key in myArray) {
    // console.log(`${key} is at index ${myArray}[key]`);

}

